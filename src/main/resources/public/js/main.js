/*
var app = angular.module("mainPage", []);

app.controller("AppCtrl", function($scope, $http){

    $http({
        method: 'GET',
        url: 'http://localhost:8080/api/payments'
    }).then(function (response){
        $scope.payments = response.data;
    },function (error){
        document.write("Some error here");
    });


});*/
